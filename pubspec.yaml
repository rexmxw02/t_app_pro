name: t_app_pro
description: Pixel perfect income tax page.
publish_to: "none"
version: 10.0.0+10

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

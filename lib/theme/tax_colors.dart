import 'package:flutter/material.dart';

class TaxColors {
  TaxColors._();

  // 依据用户安卓截图近似取样
  static const Color pageBg = Color(0xfff3f4f8);
  static const Color white = Color(0xffffffff);

  static const Color primaryBlue = Color(0xff4f84d8);

  static const Color mainText = Color(0xff303236);
  static const Color detailText = Color(0xff9da2ab);

  static const Color hairline = Color(0xffe3e4e8);
  static const Color arrow = Color(0xffc5c8ce);
}
